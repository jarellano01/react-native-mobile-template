import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    bottomMenu: {
        position: 'absolute',
        left: 0,
        bottom: 0,
        height: 100,
        width: '100%',
        backgroundColor: 'grey',
        zIndex: 100
    }
});
