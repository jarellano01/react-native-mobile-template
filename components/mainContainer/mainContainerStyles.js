import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    mainContainer: {
        position: 'relative',
        height: '100%',
        width: '100%',
        paddingTop: 100,
        paddingBottom: 100,
        backgroundColor: 'green'

    }
});
